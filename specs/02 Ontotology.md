# Ontology Design


## 繪製 Ontology 資料結構圖，使用 markdown 與 mermaid 方式輸出，並與我確認
## 檢視目前結構，增加必要 Property，使用 markdown 方式的表格輸出，並與我確認
## 檢視目前結構，建立 Cypher language 的 Create 語法，使用 markdown 方式的 code block 輸出，並與我確認
## 建立安裝 Neo4j 的 Docker 環境，並使用 docker-compose up -d 建立容器，使用 markdown 方式的 code block 輸出，並與我確認
## 建立 Neo4j 的資料庫，使用 markdown 方式的 code block 輸出，並與我確認
## 在我確認過後實際運作 neo4j docker ，並建立資料庫。

